<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sticky Notes</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <!-- input fields -->
        <div class="form p-10 text-white">
            <form action="/create" method="post" class="text-white">
                <input class="block px-5 py-2 bg-zinc-800 w-full text-start rounded-md outline-none" type="text"
                    name="title" placeholder="Title" required>
                <textarea class="block px-5 py-2 resize-none bg-zinc-800 w-full mt-5 rounded-md outline-none"
                    name="details" id="" placeholder="Write your task details" required></textarea>
                <input type="submit" class="p-2 mt-3 w-[130px] rounded-md bg-blue-600" value="Create Task">
            </form>
        </div>


        <!-- card -->

        <div class="flex gap-4 flex-wrap p-10">
            <% if (files.length> 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md text-white">
                        <h1 class="text-start font-bold text-[25px] text-red-500 tracking-tighter">
                            <%= val %>
                        </h1>
                        <a href="#" class="text-blue-500 inline-block mt-3">Read More</a>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <h1 class="text-white font-bold text-[40px] text-center">What's your tasks for today?</h1>
                            <% } %>
        </div>







    </div>

    <script src="script.js"></script>
</body>

</html>